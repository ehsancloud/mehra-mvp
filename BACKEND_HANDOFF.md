# راهنمای تحویل به بک‌اند

## معماری دیتا

تمام دیتای ماک پروژه در یک فایل واحد جمع شده تا اتصال به دیتابیس واقعی ساده و بدون ابهام باشد:

```
src/data/
  db.json   -> دیتابیس ماک (تمام entity ها: users, projects, tickets, ...)
  db.js     -> لایه‌ی نگه‌داری در حافظه (in-memory store) روی db.json
  api.js    -> تنها نقطه‌ی ورودی که کامپوننت‌ها از آن دیتا می‌گیرند
```

هیچ کامپوننتی مستقیماً به `db.json` وصل نیست؛ همه از توابع `src/data/api.js` استفاده می‌کنند
(مثل `getProjects()`, `getTickets()`, `login()`, `createProject()` و ...).
هر تابع در این فایل معادل یک endpoint آینده است و بالای هر کدام کامنت `TODO(API)`
گذاشته شده که مسیر و شکل درخواست/پاسخ پیشنهادی را توضیح می‌دهد.

## مراحل اتصال به بک‌اند واقعی

1. `db.json` و `db.js` حذف می‌شوند.
2. بدنه‌ی هر تابع در `api.js` با یک فراخوانی واقعی (`fetch`/`axios`) به endpoint متناظر
   جایگزین می‌شود — امضای تابع (نام، پارامترها، شکل خروجی) همان می‌ماند.
3. چون هیچ کامپوننتی مستقیم به دیتای ماک وصل نیست، تغییری در بقیه‌ی پروژه لازم نیست.

## نکات مهم

- `src/data/db.json` → `_meta` در ابتدای فایل توضیح کلی را دارد؛ هر بخش (`departments`,
  `users`, `projects`, `proposals`, `tickets`, `ticketMessages`, `tasks`, `reports`,
  `financeProjects`, `revenueByMonth`, ...) معادل یک جدول در دیتابیس واقعی است.
- کدهای وضعیت (status) به‌صورت انگلیسی/canonical در دیتا ذخیره شده‌اند
  (`src/utils/projectStatus.js`, `src/utils/tickets.js`, `src/utils/projectLevels.js`)
  و متن فارسی نمایشی از روی همان کد در لحظه‌ی رندر تولید می‌شود؛ یعنی بک‌اند فقط باید
  همین کدها را برگرداند و نگران متن فارسی نباشد.
- `currentSession` در `db.json` صرفاً برای شبیه‌سازی «کاربر لاگین‌شده» است تا وقتی
  احراز هویت واقعی وصل نشده. با پیاده‌سازی `login`/`register`/`completeRegistration`
  واقعی در `api.js`، این بخش باید با سشن/توکن واقعی جایگزین شود.
- ابرپروژه‌ها (`isSuperProject: true`) خودشان یک رکورد پروژه هستند که آرایه‌ی
  `subProjectIds` به چند پروژه‌ی معمولی دیگر اشاره می‌کند؛ هر زیرپروژه دقیقاً همان
  چرخه‌ی یک پروژه‌ی عادی (ارجاع، تیکت، پرداخت، مختومه شدن) را طی می‌کند.
